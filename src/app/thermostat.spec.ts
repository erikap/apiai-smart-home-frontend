/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Thermostat} from './thermostat';

describe('Thermostat', () => {
  it('should create an instance', () => {
    expect(new Thermostat()).toBeTruthy();
  });
});
